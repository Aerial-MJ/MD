# 关闭eslint

package.json里找到eslintConfig

  ```jso
  "eslintConfig": {
      "rules": {
        "no-undef": "off",
        "no-restricted-globals": "off",
        "no-unused-vars": "off"
      }
    }
  ```


