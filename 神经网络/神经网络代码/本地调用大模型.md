# 本地调用大模型
```python
self.tokenizer = AutoTokenizer.from_pretrained(
	self.model_config.name,
	**self.model_config.tokenizer_kwargs
)
self.model = AutoModelForCausalLM.from_pretrained(
	self.model_config.name,
	**self.model_config.model_kwargs
)

prompt=self.tokenizer.apply_chat_template(
	history or self.messages,
	tokenize=False,
	add_generation_prompt=True,
	enable_thinking=True
)


inputs = self.tokenizer(prompt, return_tensors="pt").to(self.model.device)
outputs = self.model.generate(
    **inputs,
    # max_length=max_length,
    max_new_tokens=50,
    temperature=temperature,
    top_p=top_p,
    do_sample=do_sample
)

```

## 声明的路径问题

把「**本地路径**」当成了「**Hub 仓库 ID**」在用。

```
HFValidationError:
Repo id must be in the form 'repo_name' or 'namespace/repo_name'
but got '../../model/Qwen/Qwen3-0.6B'
```

- **这个 API 当前模式是在「按 Hub 仓库解析」**

- **Hugging Face 有两套“语义”，混用了**

### ① Hub 模式（repo_id 语义）

```
AutoModel.from_pretrained("Qwen/Qwen3-0.6B")
```

- 从 Hugging Face Hub 下载
- 参数叫 **repo_id**
- 必须是 `namespace/repo_name`

------

### ② 本地模型模式（local path 语义）

```
AutoModel.from_pretrained("/abs/path/to/Qwen3-0.6B")
```

或

```
AutoModel.from_pretrained("./model/Qwen/Qwen3-0.6B")
```

✔ **这是完全合法的**
✔ transformers 支持
✔ 不需要联网
✔ 不需要 huggingface-cli

### 解决方法

**方式 1（最推荐）：明确告诉它：这是本地路径**

```
AutoModelForCausalLM.from_pretrained(
    "../../model/Qwen/Qwen3-0.6B",
    local_files_only=True
)
```

✔ 强制走本地
✔ 不校验 repo_id
✔ 不联网

**方式 2：用绝对路径（不是必须，但更稳）**

```
AutoModelForCausalLM.from_pretrained(
    "/data/postgraduates/2024/chenjiarui/model/Qwen/Qwen3-0.6B"
)
```

**这不是 Hugging Face 的要求**
是为了避免 `../../` 在多进程 / torchrun 下路径失效

### trust_remote_code

在使用 Hugging Face Transformers 库加载模型时，**trust_remote_code** 是一个重要的参数，用于控制是否信任从远程下载的代码。默认情况下，该参数设置为 **False**，以确保安全性，但在某些情况下需要将其设置为 **True** 来加载自定义模型或非官方模型。

当你用 **`from_pretrained` 加载 Hugging Face Hub 上的模型** 时：

- 有些模型（尤其是大模型、LLaMA、Qwen 等）会在 repo 里附带 **自定义的 Python 类/函数**（例如 `modeling_*.py` 或 `configuration_*.py`）
- Transformers 默认 **不会执行这些远程代码**，避免潜在的恶意代码

如果你设置：

```
trust_remote_code=True
```

👉 就允许 Transformers **加载和执行模型 repo 自带的自定义 Python 代码**

## tokenizer

```json
  "additional_special_tokens": [
    "<|im_start|>",
    "<|im_end|>",
    "<|object_ref_start|>",
    "<|object_ref_end|>",
    "<|box_start|>",
    "<|box_end|>",
    "<|quad_start|>",
    "<|quad_end|>",
    "<|vision_start|>",
    "<|vision_end|>",
    "<|vision_pad|>",
    "<|image_pad|>",
    "<|video_pad|>"
  ],
  "bos_token": null,
  "clean_up_tokenization_spaces": false,
  "eos_token": "<|im_end|>",
  "errors": "replace",
  "model_max_length": 131072,
  "pad_token": "<|endoftext|>",
  "split_special_tokens": false,
  "tokenizer_class": "Qwen2Tokenizer",
  "unk_token": null
}

```

### add_token使用场景

在Transformer模型输入的文本中常常会额外使用一些特殊[token]来表示一些特殊含义，比如希望对LLM通过设计prompt提升下游任务效果。

最开始在Bert预训练文本中就约定俗成用[CLS]表示句子开头、[SEP]表示隔开两个句子的符号、[UNK]表示未登录词、[PAD]表示该位置补充0。同时将特殊token加入模型词汇表vocab后可以防止tokenzier在切词时将该[token]切开，在文本编码时保留该[token]为一个整体。

常见使用场景在Bert表征文本时，你需要再使用一些特殊的[token]加入输入的文本中，能起到额外补充一些信息的作用。当然效果好的前提是需要足够多数据让模型finetune。如果数据量少，不如采用词表中已有的特殊[token]去做训练。

### 实现方法

常见实现方式有以下三种：

#### 1.修改vocab.txt

直接修改词表vocab.txt，具体通过替换其中[unuesd]的token。

#### 2.tokenizer.add_tokens()

通过tokenizer.add_tokens() 添加新的tokens在tokenizer中，再使用model.resize_token_embeddings() 随机初始化权重。

####  3.tokenizer.add_special_tokens()

通过tokenizer.add_special_tokens() 添加新的 special tokens在tokenizer中，再使用model.resize_token_embeddings() 随机初始化权重。

## model and tokenizer

```python
self.tokenizer = AutoTokenizer.from_pretrained(
	self.model_config.name,
	**self.model_config.tokenizer_kwargs
)
self.model = AutoModelForCausalLM.from_pretrained(
	self.model_config.name,
	**self.model_config.model_kwargs
)


#  tokenizer_kwargs:
#    trust_remote_code: true
#  model_kwargs:
#    trust_remote_code: true
#    dtype: "auto"
#    device_map: "auto"
```

## model.prompt

```python
prompt = self.tokenizer.apply_chat_template(
    history or self.messages,
    tokenize=False,
    add_generation_prompt=True,
    enable_thinking=True
)

inputs = self.tokenizer(prompt, return_tensors="pt").to(self.model.device)

outputs = self.model.generate(
	**inputs,
	max_new_tokens=max_new_tokens,
	temperature=temperature,
	top_p=top_p,
	do_sample=do_sample,
	pad_token_id=self.tokenizer.eos_token_id
)
```

### tokenize

决定 **输出的是字符串，还是 token id**

- `tokenize=False` → **输出字符串 prompt**
- `tokenize=True` → **直接输出 input_ids（tensor）**

因为后面通常会：

```
tokenizer(
    prompt,
    return_tensors="pt",
    ...
)
```

也就是：

> **先拼好完整对话文本 → 再统一 tokenize**

### add_generation_prompt

是否添加提示词表示需要模型生成回复 (默认False)

**add_generation_prompt=True:**

```python
<|im_start|>system
You are a helpful AI assistant. Provide accurate and detailed answers to questions.<|im_end|>
<|im_start|>user
什么是糖尿病？<|im_end|>
<|im_start|>assistant
<think>
```

**add_generation_prompt=false:**

```
<|im_start|>system
You are Alice, a lively young girl<|im_end|>
<|im_start|>user
Problem: How to solve global warming? We need to discuss and find the most effective short-term solution.Use least word to answer<|im_end|>
<|im_start|>
<|im_start|>
Okay, the user wants a short answer to the problem of solving global warming with the least words possible. Let me start by recalling the main points. Global warming is caused by greenhouse gases, so the immediate steps would be reducing emissions. I should mention reducing carbon footprints, like using public transport and electric vehicles. Also, supporting renewable energy sources. That's the most effective short-term solutions. Need to keep it concise but cover the key points without being too lengthy. Let me check the word count to make sure it's minimal.
</think>
```

### enable_thinking

```
<|assistant|>
<think>
...
</think>
```

**enable_thinking=false**：

```python
<|im_start|>system
You are Alice, a lively young girl<|im_end|>
<|im_start|>user
Problem: How to solve global warming? We need to discuss and find the most effective short-term solution.Use least word to answer<|im_end|>
<|im_start|>assistant
<think>

</think>

以上都是input//I don't think we need to solve global warming. Let me know if there's something else I can help with! 😊<|im_end|>
```

- **先输出思考过程**
- **再输出最终答案**

## model.generate

```python
outputs = self.model.generate(
	**inputs,
	# max_length=max_length,
	max_new_tokens=50,
	temperature=temperature,
    top_p=top_p,
	do_sample=do_sample
)
```

### do_sample

当 **do_sample** 设置为 **True** 时，意味着模型在生成文本时将采用**随机采样**的方式。这种设置允许模型根据下一个单词的概率分布进行采样，从而增加生成文本的多样性。相对而言，设置为 **False** 时，模型将选择概率最高的单词作为输出，这样生成的文本会更加确定性和一致性。

### top-k采样

是指在每一步生成时，只保留概率最高的前k个单词，然后从这些单词中随机选择一个作为输出。这种方法可以限制输出序列的长度，同时保持一定的多样性。例如，如果k=3，模型将从概率最高的三个单词中选择一个。

### top_p

是指在每一步生成时，从累积概率超过某个阈值p的最小单词集合中进行随机采样。这种方法只关注概率分布的核心部分，忽略尾部部分。

### temperature

**温度越高越随机**

“采样温度”是在0到2之间选择的参数。较高的值（如0.8）会使输出更具随机性，而较低的值（如0.2）则会使输出更集中，更确定性。

当 temperature 设置为较高值（接近1或大于1）时，模型在选择下一个 token 时，会倾向于选择概率较低的选项，从而产生更具创新性和多样性的输出。而当 temperature 设置为较低值（接近0但大于0）时，模型在选择下一个 token 时，会更偏向于选择概率较高的选项，从而产生更准确、更确定性的输出。可以将其比喻为一个热力调节器：温度越高，模型的输出就越'热闹'，有更多的可能性和随机性；温度越低，输出就越'冷静'，更加保守和确定。

## 属性注解

**Python 的“属性注解”就是：
 用 `:` 给类属性 / 实例属性标注“类型信息”，本身不创建值、不做赋值，只是说明。**

👉 **注解 ≠ 赋值**

### 普通赋值（真的创建属性）

```python
class A:
    x = 1
```

- `x`：类属性
- 有值：`1`
- `A.x == 1`

------

### 属性注解（只声明类型，不给值）

```python
class A:
    x: int
```

- `x` **没有值**
- 只是说：`x` 应该是 `int`
- **不会创建 `A.x`**

```python
>>> hasattr(A, "x")
False
```

但类型信息会进这里：

```python
>>> A.__annotations__
{'x': <class 'int'>}
```

### 错误理解（以为这样就有属性了）

```
class A:
    def __init__(self):
        self.lr: float
```

❗ 这是 **只注解，不赋值**

```
>>> a = A()
>>> a.lr
AttributeError
```

✔️ 正确写法：

```
class A:
    def __init__(self):
        self.lr: float = 1e-4
```

## dataclass 中的属性注解（重点）

### dataclass 的规则

```
@dataclass
class Config:
    lr: float = 2e-5
    batch_size: int = 4
```

dataclass 会做三件事：

1. **只看有注解的字段**
2. 生成 `__init__`
3. 把默认值塞进去

等价于：

```
def __init__(self, lr=2e-5, batch_size=4):
    self.lr = lr
    self.batch_size = batch_size
```

## 属性注解 vs field(default_factory=...)

你之前也问过这个，这里统一解释：

```
from dataclasses import dataclass, field

@dataclass
class A:
    x: list = field(default_factory=list)
```

### 为什么不用 `x: list = []`？

因为：

```
class A:
    x = []
```

是 **类属性共享**

而 `default_factory` 是：

> “每个实例生成一个新的对象”

## 一些问题

### clip_grad_norm_

```python
torch.nn.utils.clip_grad_norm_(self.agent.model.parameters(), 1.0)
```

**如果所有参数的梯度整体范数 > 1.0，就按比例缩小**

公式是：

```
g ← g * (max_norm / ||g||)
```

因为 LLM 非常容易：

- 某一层 attention 爆梯度
- 导致一次 step 把模型参数“拉飞”

clip 的作用是：“我允许你更新，但不允许你一次走太大步”

### rnn.pad_sequence

```python
"input_ids": torch.nn.utils.rnn.pad_sequence(
	[item["input_ids"] for item in x],
	batch_first=True,
	padding_value=agent.tokenizer.pad_token_id or agent.tokenizer.eos_token_id
)
```

`torch.nn.utils.rnn.pad_sequence` 是 **PyTorch 提供的一个“序列对齐（padding）工具函数”**，专门用来解决一个核心问题：

**一个 batch 里的序列长度不一样，没法直接堆成张量**



### FP16



### mask system / user loss



### 因果语言模型（Causal LM）

**会出现训练模型复述问题**

你现在的 loss 计算是：

```
loss on:
- system tokens
- user tokens
- assistant tokens
```

但正确做法应该是：

**只在 assistant 部分算 loss**

这是 **Chat SFT 的标准做法**
（LLaMA / Qwen 官方训练都是这样）













